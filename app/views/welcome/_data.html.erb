<% unless taken %>
  <%= link_to "buy now", "#.#{tld}", :class => 'btn btn-success' %>
<% else %>
  <%= link_to "make offer", "#.#{tld}", :class => 'btn btn-danger' %>
  <%= link_to "whois", whois_path(q: "#.#{tld}"), :class => 'btn btn-danger', :remote => true %>
<% end %>
